using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParkingCheck : MonoBehaviour
{
    /*private bool holdPlace;
    private GameObject place;
    private SpawnPlayers spawnPlayers;
    private void Start()
    {
        GameObject.Find("GameManager").TryGetComponent(out SpawnPlayers _spawnPlayers);
        spawnPlayers = _spawnPlayers;
        place = GetComponent<GameObject>();
        var a = place.GetComponent<Collider>();
    }


    private void OnTriggerEnter(Collider other)
    {
        
        if (other.CompareTag("Car"))
        {

            this.gameObject.GetComponent<BoxCollider>().enabled = false;
        }
        else return;
       
    }
    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("Car"))
        {
            this.gameObject.GetComponent<BoxCollider>().enabled = true;
        }
        else return;
    }*/
}
